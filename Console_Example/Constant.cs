using System;

namespace Console_Example
{
    enum SORT
    {
        ASC = 1,
        DECS = 2
    }

    enum CODE
    {
        NUMBER = 1,
        STRING = 2
    }

    enum METHOD
    {
        SORT = 1,
        LINQ = 2
    }

    enum DUPLICATION
    {
        DISTINCT = 1,
        NOT = 2
    }

    enum CONVERT
    {
        ASC = 100,
        DECS = 200,
        NUMBER = 300,
        STRING = 400,
        DISTINCT = 500,
        NOT = 600
    }
}
